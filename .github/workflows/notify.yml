name: notify
run-name: Notify about a Pull Request if all checks are passed

on:
  pull_request:

jobs:
  dummy_job:
    runs-on: ubuntu-latest
    steps:
      - name: Dummy Step
        run: echo "This is a dummy job to satisfy GitHub's requirements"
  notify:
    runs-on: ubuntu-latest
    needs: [job1, job2]
    if: always()
    steps:
      - name: Final Step
        run: echo "All previous jobs were successful"
